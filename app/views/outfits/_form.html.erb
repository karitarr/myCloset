<%= form_for(@outfit) do |f| %>
  <% if @outfit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@outfit.errors.count, "error") %> prohibited this outfit from being saved:</h2>

      <ul>
      <% @outfit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :published_on %><br>
    <%= f.date_select :published_on %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>

  <div class="field">
  <%= f.label :clothing_ids %><br />
  <%= f.select :clothing_ids, Clothing.all.collect {|a| [a.name, a.id] }, {}, :multiple => true %>
  </div>

  <div class="field">
  <%= f.label :clothing_ids_2 %><br />
  <%= f.select :clothing_ids, Clothing.all.collect {|a| [a.name, a.id]  }, {}, :multiple => true %>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
